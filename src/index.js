// src/index.js
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Renders the <App /> component into the <div id="root"></div> in index.html
ReactDOM.render(<App />, document.getElementById('root'));
